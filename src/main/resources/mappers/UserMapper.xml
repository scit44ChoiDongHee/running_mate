<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.softsociety.running_mate.dao.UserDAO">

	<insert id="save" parameterType="Int">
		INSERT INTO 
			NumberTable (id, value)
		VALUES 
			(NumberSeq.NEXTVAL, #{testInput})
	</insert>

<insert id="userSingUp" parameterType="UserInfo">
    insert into
    userInfo (user_id, user_pw, user_name, user_email, user_birthday, user_phone, user_nickname, user_photo)
    values
    (#{user_id}, #{user_pw}, #{user_name}, #{user_email}, #{user_birthday}, #{user_phone}, #{user_nickname}, #{user_photo})
</insert>


</mapper>